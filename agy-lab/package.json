{
  "name": "agy-lab",
  "version": "0.2.0",
  "description": "Install, authenticate and drive the Antigravity CLI and signed-in ChatGPT browser sessions inside a container.",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "node src/server.ts",
    "test": "node --test src/*.test.ts"
  },
  "engines": { "node": ">=24" },
  "dependencies": {
    "patchright": "^1.61.1",
    "pg": "^8.16.3"
  }
}
